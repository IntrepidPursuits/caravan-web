import { combineReducers } from 'redux';

const reducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export const caravanApp = combineReducers({
  reducer
})
